name: CI-CD

on : [push]

jobs :
    build:
        runs-on: ubuntu-latest
        steps:
            - name : Checkout code
              uses : actions/checkout@v4
            - name : Hello
              run : echo "Hello from Github Actions!"
